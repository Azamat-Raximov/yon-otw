# Netlify-style redirects for SPA
# Some crawlers still request /og-image.png; force it to the current asset.
/og-image.png   /og-image.jpg   301!

# All routes should fall through to index.html for client-side routing
/*    /index.html   200
